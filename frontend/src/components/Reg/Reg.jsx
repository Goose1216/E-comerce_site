import regStyles from '../../styles/RegWindow/RegStyles.module.css'

const Reg = () => {
    return (
        <div className={regStyles.Body}>
            <h1 className={regStyles.Auth}>Авторизация</h1>
            <div className={regStyles.Window}>
            </div>
        </div>
    );
};

export default Reg;